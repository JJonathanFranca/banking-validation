apiVersion: v1
kind: Service
metadata:
  name: postgres-validation
  namespace: banking-service
spec:
  selector:
    app: postgres-validation
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432